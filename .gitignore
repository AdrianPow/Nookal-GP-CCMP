# Local config — contains the live Nookal API key. Never commit.
nookal_config.json

# Run output
diagnostic_report.txt
nookal_audit.jsonl

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Review queue and referral inbox — contains patient data. Never commit.
queue/
inbox/
